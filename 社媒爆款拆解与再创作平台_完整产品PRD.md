# 社媒爆款拆解与再创作平台｜完整产品PRD

当前实施版本：V1.1
日期：2026-09-23

> 文档定位：**完整产品框架 + 当前 V1.1 实施范围 + 后续版本预留**。V1.1 是当前开发实施版本，不代表本文档仅包含 V1。

> 核心链路：**找依据 → 拆解 → 再创作 → 封面（可选）→ 复制 / 导出**。面向中国大陆用户，V1 支持小红书、抖音。

## 1. 产品定位

不是普通“空白写作 AI”，而是“有依据创作”：用户输入单条链接、批量链接、博主或关键词，系统先从真实公开内容取得依据，再按平台模板拆解并生成用户自己的可直接复制内容。

再创作先定这篇内容的岗位，再写句子。参考笔记提供被验证过的机制；一次写出 6 到 8 条，这一批不能都停在对参考句子的换词仿写。

V1 以工具为主，后续逐步升级为 AI 内容员工。

## 2. V1 必做

- 邮箱登录（管理员配置 SMTP 后：注册需邮箱验证码，支持验证码找回密码；未配置时管理员可在后台重置用户密码）。
- 一个用户多个项目，项目数据隔离。
- 四个入口：单条链接、批量链接、博主、关键词/品类。
- 小红书与抖音使用独立拆解模板。
- 每条独立拆解；不强制做共性研究报告。
- “创作我的版本”：参考内容 + 可选项目资料/临时资料 + 本次要求；一次生成 6 到 8 条独立稿件。
- 同一条参考链接可多次再创作；每次新增 6 到 8 条结果，不覆盖历史稿件，用户可单条删除。
- 小红书/需要封面的内容可选生成封面，系统先给 2–3 个方向，用户不写 Prompt。
- 项目资料支持新增、更新/替换、删除；临时上传后可选择是否保存。
- 默认不长期存完整原视频。
- 一键复制；Excel/CSV 统一结构导出，可导入飞书表格/多维表格。
- 积分底层、流水、失败退款先建好；V1 可不接正式支付。
- 超级管理员：用户、积分规则、任务、TikHub、模型、模型同步/测试、主备模型、Skill/Prompt 版本。

## 3. V1 不做

- 自动发布。
- 最终视频生成。
- 团队/企业版。
- 复杂 AI 员工和持续监控。
- 前台模型选择。
- 后台自动搜索 GitHub Skill。

## 4. 关键原则

1. 能系统判断的，不让用户选。
2. 不展示内部 Prompt、算法原理和模型参数。
3. 参考内容必须先存在，V1 不提供纯空白创作入口。
4. 小红书、抖音平台分治。
5. 分析结果先看再保存，不默认污染项目。
6. 重操作积分化；封面单独计费；失败退款。
7. 模型、数据源、Skill 均 Adapter/配置化，禁止写死。

## 5. 用户主流程

1. 选择/新建项目。
2. 单条 / 批量 / 博主 / 关键词。
3. 自动识别平台，TikHub 获取公开数据。
4. 按平台 Skill 拆解。
5. 用户浏览，选择是否保存。
6. “创作我的版本”。
7. 可选项目资料、临时资料、本次要求。
8. 一次生成 6 到 8 条独立新稿，并以卡片/列表方式追加展示。
9. 同一参考内容可再次生成；新结果继续追加，不覆盖旧稿。
10. 用户可单条复制、保存或删除任意再创作稿。
11. 可选生成封面。
12. 复制或导出。

## 6. 低粉高曝 / 异常爆款

不要用一个固定粉丝阈值横跨两平台。使用相对异常度：

- 内容表现强度；
- 曝光/互动 ÷ 粉丝的粉丝效率；
- 当前内容 ÷ 账号近期中位数；
- 当前内容 ÷ 同赛道样本中位数；
- 新鲜度；
- 可复用性。

权重后台配置，真实样本校准。

实现口径（2026-09-26）：
- 博主入口：以该博主近期作品的互动中位数作为账号基线；关键词入口：以同一批搜索结果的互动中位数作为赛道基线。互动 = 点赞 + 2×收藏 + 3×评论。
- 两个入口都先按相对表现排序，再精选前 N 条（N 后台可配，默认 3）拆解；不是按接口原顺序截取。
- 按精选条数先冻结积分，成功几条结算几条，其余退回；部分失败时任务状态为 `PARTIAL`。
- 校准工具：`scripts/calibrate_ranking.py` 读取 30–50 条人工标注样本，输出各维度 AUC 与建议权重，人工确认后在后台保存。

## 7. 小红书拆解 Schema

基础信息、选题、标题、封面、开头、正文结构、表达、互动设计、爆点判断、可迁移规则。

## 8. 抖音拆解 Schema

基础信息、核心选题、前三秒钩子、脚本结构、口播与字幕、镜头结构、节奏、互动/转化、爆点判断、可迁移规则。

口播与字幕的依据按优先级：用户在单条入口粘贴的口播/字幕文字 → 后台配置转写模型时，临时下载视频（≤25 MB，只在内存中，不落盘不入库）调用 `/audio/transcriptions` 自动转写 → 都没有时，口播、镜头、节奏须标注「无法判断」，不得假装看过视频。

## 9. 再创作输出

### 9.0 多稿生成与版本保留
- 一条参考链接进入“创作我的版本”后，一次生成 **6 到 8 条**独立稿件。这是单次批量，不是每日上限。同一条参考可以反复再点，每次再追加 6 到 8 条。少于 6 条不算完整的一批，多于 8 条超出这一批的工序。
- 这 6 到 8 条稿件必须先各自标明内容方向，再写表达。方向可以不同；同一方向下也必须是不同切角，而不是只替换少量词语。参考机制可以复用，但不能让这一批都停在对参考句子的仿写。
- 用户可以对同一条参考内容再次点击生成；每次继续新增 6 到 8 条，**历史结果不覆盖、不替换**。
- 每一条生成结果都必须拥有独立 `creation_output_id`、创建时间、所用 Skill 版本与模型任务记录。
- 前台采用卡片/列表追加展示，支持单条：复制、保存/收藏、生成封面、删除。
- 用户删除某一条只影响该条结果，不影响同批另外两条，也不影响参考拆解和后续继续生成。
- 删除建议采用软删除；V1 前台可以直接表现为删除，后台保留审计所需最小记录。
- 已完成生成后用户主动删除，不退还积分；模型/任务失败则仍按失败退款规则处理。


### 9.1 创作方向（小红书先落地）

来源是口播《郑州帮不会死，只会进化》。它纠正的是「找一条不为人知的爆款秘诀」。结论是：核心不是某一条内容技巧，也不是流量玄学，而是把内容生产做成可执行的工序。护城河是组织力，不是创意才华。

因此创作不能只有「机制一致、表达不同」。下面的规则已写成创作 Skill v5（以草稿写入，需在后台测试并发布后生效），并落进 JSON Schema 与结构校验：每条稿必须有 `direction` / `role` / `angle`，小红书必须有 `comment_layout`。

小红书先使用视频里写明的两种方向，不另造第三种：

| 方向 | 这篇笔记的岗位 | 写法 |
| --- | --- | --- |
| 测评 | 建立权威和信任，帮人做决定 | 比较、标准、结论。只使用资料和参考里已有的事实 |
| 钓鱼帖 | 截流，把泛流量引进来 | 用一个普通人会问出口的问题或场景开场，把讨论引到产品能回答的点上 |

共同标准是「好生产、好复制」：选题、结构、配图都要能被重复做出来。不把稿件写成依赖某一个有网感的人现场发挥。项目资料里如果写了语气，语气服从资料；调性和人设不能拿来代替内容方向。

这一批稿的分配：

- 资料够用时，同一批里测评和钓鱼帖都要出现，不能整批都是同一岗位的换说法。
- 资料只够支撑一个方向时，这一批可以同方向，但每条必须是不同切角，并在 `missing` 里写明另一个方向缺哪项事实。
- 钓鱼帖只生成用户自己账号能发的提问式笔记。不指示用户再开一个号假装路人来回答，不编造「回购五次」这类经历。

### 9.2 评论布局是创作结果的一部分

视频的铁律是：三分内容，七分评论。笔记正文可以平，信任和购买指向放在评论区。发布后第一个小时要完成第一波评论布局。小红书稿件因此增加「评论布局」，三个职能都要给到可复制的句子：

- 气氛：调动情绪，出现「这个我也用过，真的不错」这类从众讨论。没有真实使用经历就不要写成已发生的事实，改成可以向读者提出的问题。
- 知识：用路人能看懂的话，补上资料里已经有的卖点细节。
- 置顶：写清第一条最该被看见的评论要完成什么，以及如何把它留在最前面。店铺名、购买入口只能使用资料里真实存在的信息。

视频里的气氛组、知识组、盖楼组，在产品里落成上面三种职能。不生成多账号对敲、虚假身份互评，或伪装成路人的店铺广告。

### 9.3 矩阵和投放：先记进创作原则，V1 不自动执行

- 单篇、单账号、单品都有天花板。一条参考不应只变成一篇笔记的几种说法。视频里的做法是多篇笔记、多个品牌、多个店铺齐头并进：这条数据不行，还有别的在跑，用数量对抗单品天花板和推荐的不确定性。V1 用一次 6 到 8 条体现这种「还有别的在跑」，不在 V1 自动开矩阵账号。
- 投放不是创作步骤，也不能用来拯救没人看的稿。顺序是先沉淀内容，用自然流看数据；点击率能稳定在 20% 以上、看得出有机会成为千赞测评时，才做小额保护性投放，放大已经被验证的结果。V1 不接投放。导出和稿件说明里保留这句，避免把生成本身当成投流理由。

抖音稿仍按脚本、分镜、拍摄清单交付。同一原则适用：先写明这条视频是在建立信任还是在截流，再写口播。视频没有给出另一套抖音内容类型，不在文档里补造。

### 小红书
- 内容方向：每条稿标明「测评」或「钓鱼帖」，并写明这篇的岗位
- 3 个标题候选
- 封面文案
- 可复制正文
- 标签/话题
- 配图建议
- 评论布局：气氛、知识、置顶各给可复制句子，并说明发布后一小时内要完成的第一波
- 必要时提示素材缺口，禁止编造事实

### 抖音
- 内容任务：每条稿标明「建立信任」或「截流」
- 标题/发布文案
- 前三秒钩子（台词+画面）
- 完整脚本
- 分镜
- 拍摄清单
- 可选 AI 视频镜头提示（不生成最终视频）

## 10. 资料库

项目长期资料 + 本次临时资料。长期资料可含品牌、产品、受众、禁用词、图片等。创作时不自动全选。临时资料任务后询问是否保存。支持上传 TXT / Markdown / CSV / Word（.docx）/ PDF 自动提取文字，用户核对后保存；扫描件 PDF 需手动粘贴。

## 11. 封面

文案完成后可选。系统自动给 2–3 个方向 → 用户选方向 → 编辑标题/副标题/清单并选用项目图片 → 免费预览 → 保存时单独计费。前台不出现模型名和 Prompt。

实现口径（2026-09-26）：V1 使用本地排版渲染（品牌杂志 / 方法清单 / 真人冲击三个方向，1080×1440 PNG/JPG），不调用图像模型重绘，原图不被改动，因此不会编造产品外观。图像模型适配器保留在后台「封面模型」配置中，作为后续版本的生成方式；是否在 V1 启用由产品决定。

## 12. 积分

V1 可人工发积分、不接支付。操作前展示预计消耗，执行时冻结，成功结算，失败自动退。批量逐条结算。管理员可动态调整积分规则和用户余额。

## 13. 超级管理员

- 用户管理
- 积分与流水
- 任务与错误/重试
- TikHub 数据源设置与测试
- 模型供应商、Base URL、Key、模型ID、启停
- 模型列表同步（供应商支持时）
- API 连接测试
- 拆解/再创作/封面的主模型与备用模型
- Skill/Prompt：Draft → Test → Published → Rollback
- 系统配置：批量上限、超时、重试、临时文件生命周期等

## 14. 核心数据表

`users`, `projects`, `assets`, `source_contents`, `tasks`, `analysis_outputs`, `creation_outputs`（每条稿件一行/一对象，禁止覆盖历史结果）, `generated_images`, `credit_accounts`, `credit_ledger`, `credit_rules`, `model_providers`, `model_routes`, `skills`, `skill_versions`。

## 15. 状态机

`PENDING → FETCHING → PROCESSING → ANALYZING / GENERATING → SUCCEEDED`，另有 `PARTIAL / FAILED / CANCELLED`。

## 16. TikHub 接入

使用 DataSource Adapter。建议统一接口：

- `resolveShareLink()`
- `getContentDetail()`
- `getCreatorProfile()`
- `listCreatorContents()`
- `searchContents()`
- `getTrending()`

TikHub 2026-06-29 小红书指南显示 App V2 为推荐系列，可用分享链接或 ID 读取笔记详情、用户信息与用户发布笔记列表。第三方接口、字段和价格会变，endpoint 不得散落硬编码。

## 17. GitHub 参考（仅开发阶段）

- `Youhai020616/xiaohongshu`
- `Jinsong-Zhou/xhs-autopilot`
- `EthanYoQ/agent-xiaohongshu-workbench`
- `ltppp/douyin-ingest`
- `lid664951-crypto/douyin-creator-toolkit`

用于借鉴 Schema、流程和失败处理，再改写为本产品 Skill。线上后台不做 GitHub Skill 搜索/安装。

## 18. 开发顺序

1. 数据模型、多租户权限。
2. 邮箱登录、项目、资料库。
3. Provider Adapter + TikHub；先单条小红书，再单条抖音。
4. Queue、状态机、重试、积分冻结/退款。
5. 拆解 Skill + JSON Schema。
6. 再创作：一批 6 到 8 条、结果追加不覆盖、支持单条删除。
7. 批量。
8. 博主与关键词精选。
9. 封面生成。
10. Excel/CSV 导出。
11. 超级后台。
12. 30–50 组真实样本回归测试并校准排序。

## 19. 外部核验资料

- TikHub 小红书 API 使用指南：https://blog.tikhub.io/zh/article/7
- TikHub Getting Started：https://tikhub.io/getting-started
- TikHub 抖音/热点示例：https://blog.tikhub.io/zh/article/hello-world

> 第三方接口会变化，开发时以实际 OpenAPI 文档和账号可用接口为准。
