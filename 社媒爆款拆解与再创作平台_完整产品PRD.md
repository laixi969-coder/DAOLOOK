# 社媒爆款拆解与再创作平台｜完整产品PRD

当前实施版本：V1.1
日期：2026-09-23

> 文档定位：**完整产品框架 + 当前 V1.1 实施范围 + 后续版本预留**。V1.1 是当前开发实施版本，不代表本文档仅包含 V1。

> 核心链路：**找依据 → 拆解 → 再创作 → 封面（可选）→ 复制 / 导出**。面向中国大陆用户，V1 支持小红书、抖音。

## 1. 产品定位

不是普通“空白写作 AI”，而是“有依据创作”：用户输入单条链接、批量链接、博主或关键词，系统先从真实公开内容取得依据，再按平台模板拆解并生成用户自己的可直接复制内容。

V1 以工具为主，后续逐步升级为 AI 内容员工。

## 2. V1 必做

- 邮箱登录。
- 一个用户多个项目，项目数据隔离。
- 四个入口：单条链接、批量链接、博主、关键词/品类。
- 小红书与抖音使用独立拆解模板。
- 每条独立拆解；不强制做共性研究报告。
- “创作我的版本”：参考内容 + 可选项目资料/临时资料 + 本次要求；一次默认生成 3 条独立稿件。
- 同一条参考链接可多次再创作；每次新增 3 条结果，不覆盖历史稿件，用户可单条删除。
- 小红书/需要封面的内容可选生成封面，系统先给 2–3 个方向，用户不写 Prompt。
- 项目资料支持新增、更新/替换、删除；临时上传后可选择是否保存。
- 默认不长期存完整原视频。
- 一键复制；Excel/CSV 统一结构导出，可导入飞书表格/多维表格。
- 积分底层、流水、失败退款先建好；V1 可不接正式支付。
- 超级管理员：用户、积分规则、任务、TikHub、模型、模型同步/测试、主备模型、Skill/Prompt 版本。

## 3. V1 不做

- 自动发布。
- 最终视频生成。
- 团队/企业版。
- 复杂 AI 员工和持续监控。
- 前台模型选择。
- 后台自动搜索 GitHub Skill。

## 4. 关键原则

1. 能系统判断的，不让用户选。
2. 不展示内部 Prompt、算法原理和模型参数。
3. 参考内容必须先存在，V1 不提供纯空白创作入口。
4. 小红书、抖音平台分治。
5. 分析结果先看再保存，不默认污染项目。
6. 重操作积分化；封面单独计费；失败退款。
7. 模型、数据源、Skill 均 Adapter/配置化，禁止写死。

## 5. 用户主流程

1. 选择/新建项目。
2. 单条 / 批量 / 博主 / 关键词。
3. 自动识别平台，TikHub 获取公开数据。
4. 按平台 Skill 拆解。
5. 用户浏览，选择是否保存。
6. “创作我的版本”。
7. 可选项目资料、临时资料、本次要求。
8. 一次默认生成 3 条独立新稿，并以卡片/列表方式追加展示。
9. 同一参考内容可再次生成；新结果继续追加，不覆盖旧稿。
10. 用户可单条复制、保存或删除任意再创作稿。
11. 可选生成封面。
12. 复制或导出。

## 6. 低粉高曝 / 异常爆款

不要用一个固定粉丝阈值横跨两平台。使用相对异常度：

- 内容表现强度；
- 曝光/互动 ÷ 粉丝的粉丝效率；
- 当前内容 ÷ 账号近期中位数；
- 当前内容 ÷ 同赛道样本中位数；
- 新鲜度；
- 可复用性。

权重后台配置，真实样本校准。

## 7. 小红书拆解 Schema

基础信息、选题、标题、封面、开头、正文结构、表达、互动设计、爆点判断、可迁移规则。

## 8. 抖音拆解 Schema

基础信息、核心选题、前三秒钩子、脚本结构、口播与字幕、镜头结构、节奏、互动/转化、爆点判断、可迁移规则。

## 9. 再创作输出

### 9.0 多稿生成与版本保留
- 一条参考链接进入“创作我的版本”后，默认一次生成 **3 条**独立稿件。
- 3 条稿件必须是同一参考机制下的不同表达方案，而不是只替换少量词语。
- 用户可以对同一条参考内容再次点击生成；每次继续新增 3 条，**历史结果不覆盖、不替换**。
- 每一条生成结果都必须拥有独立 `creation_output_id`、创建时间、所用 Skill 版本与模型任务记录。
- 前台采用卡片/列表追加展示，支持单条：复制、保存/收藏、生成封面、删除。
- 用户删除某一条只影响该条结果，不影响同批另外两条，也不影响参考拆解和后续继续生成。
- 删除建议采用软删除；V1 前台可以直接表现为删除，后台保留审计所需最小记录。
- 已完成生成后用户主动删除，不退还积分；模型/任务失败则仍按失败退款规则处理。


### 小红书
- 3 个标题候选
- 封面文案
- 可复制正文
- 标签/话题
- 配图建议
- 必要时提示素材缺口，禁止编造事实

### 抖音
- 标题/发布文案
- 前三秒钩子（台词+画面）
- 完整脚本
- 分镜
- 拍摄清单
- 可选 AI 视频镜头提示（不生成最终视频）

## 10. 资料库

项目长期资料 + 本次临时资料。长期资料可含品牌、产品、受众、禁用词、图片等。创作时不自动全选。临时资料任务后询问是否保存。

## 11. 封面

文案完成后可选。系统自动给 2–3 个方向 → 用户选方向 → 调用图像模型。前台不出现模型名和 Prompt。

## 12. 积分

V1 可人工发积分、不接支付。操作前展示预计消耗，执行时冻结，成功结算，失败自动退。批量逐条结算。管理员可动态调整积分规则和用户余额。

## 13. 超级管理员

- 用户管理
- 积分与流水
- 任务与错误/重试
- TikHub 数据源设置与测试
- 模型供应商、Base URL、Key、模型ID、启停
- 模型列表同步（供应商支持时）
- API 连接测试
- 拆解/再创作/封面的主模型与备用模型
- Skill/Prompt：Draft → Test → Published → Rollback
- 系统配置：批量上限、超时、重试、临时文件生命周期等

## 14. 核心数据表

`users`, `projects`, `assets`, `source_contents`, `tasks`, `analysis_outputs`, `creation_outputs`（每条稿件一行/一对象，禁止覆盖历史结果）, `generated_images`, `credit_accounts`, `credit_ledger`, `credit_rules`, `model_providers`, `model_routes`, `skills`, `skill_versions`。

## 15. 状态机

`PENDING → FETCHING → PROCESSING → ANALYZING / GENERATING → SUCCEEDED`，另有 `PARTIAL / FAILED / CANCELLED`。

## 16. TikHub 接入

使用 DataSource Adapter。建议统一接口：

- `resolveShareLink()`
- `getContentDetail()`
- `getCreatorProfile()`
- `listCreatorContents()`
- `searchContents()`
- `getTrending()`

TikHub 2026-06-29 小红书指南显示 App V2 为推荐系列，可用分享链接或 ID 读取笔记详情、用户信息与用户发布笔记列表。第三方接口、字段和价格会变，endpoint 不得散落硬编码。

## 17. GitHub 参考（仅开发阶段）

- `Youhai020616/xiaohongshu`
- `Jinsong-Zhou/xhs-autopilot`
- `EthanYoQ/agent-xiaohongshu-workbench`
- `ltppp/douyin-ingest`
- `lid664951-crypto/douyin-creator-toolkit`

用于借鉴 Schema、流程和失败处理，再改写为本产品 Skill。线上后台不做 GitHub Skill 搜索/安装。

## 18. 开发顺序

1. 数据模型、多租户权限。
2. 邮箱登录、项目、资料库。
3. Provider Adapter + TikHub；先单条小红书，再单条抖音。
4. Queue、状态机、重试、积分冻结/退款。
5. 拆解 Skill + JSON Schema。
6. 再创作：默认一批 3 条、结果追加不覆盖、支持单条删除。
7. 批量。
8. 博主与关键词精选。
9. 封面生成。
10. Excel/CSV 导出。
11. 超级后台。
12. 30–50 组真实样本回归测试并校准排序。

## 19. 外部核验资料

- TikHub 小红书 API 使用指南：https://blog.tikhub.io/zh/article/7
- TikHub Getting Started：https://tikhub.io/getting-started
- TikHub 抖音/热点示例：https://blog.tikhub.io/zh/article/hello-world

> 第三方接口会变化，开发时以实际 OpenAPI 文档和账号可用接口为准。
